<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
	<select id="noticeFive" resultType="com.es.notice.NoticeDataBean">
		select * from (select * from notice order by now desc where rownum <=5);
	</select>
		<select id="eductionhistoryFive" resultType="com.es.education.EduHistoryDao">
		select * from (select * from education_history order by now desc where rownum <=5);
	</select>	
		<select id="selectFive" resultType="com.es.notice.NoticeDataBean">
		select * from (select * from notice order by now desc where rownum <=5);
	</select>	
		<select id="selectFive" resultType="com.es.notice.NoticeDataBean">
		select * from (select * from notice order by now desc where rownum <=5);
	</select>		
</mapper>